# Database connection string (e.g. for PostgreSQL in production or local development)
DATABASE_URL="postgresql://username:password@localhost:5432/expense_tracker?schema=public"

# JWT Secret for signing session cookies (generate a secure random 32-character string)
JWT_SECRET="expense_tracker_secret_key_2026_super_secure_9988"
