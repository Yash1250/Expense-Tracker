import LoginClient from '@/components/LoginClient';

export const metadata = {
  title: 'Login - Expense Tracker',
  description: 'Sign in to access your financial portfolio',
};

export default function LoginPage() {
  return <LoginClient />;
}
